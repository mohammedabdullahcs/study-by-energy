# Supabase Configuration (Optional)
# 
# Leave these empty to use StudyByEnergy locally without cloud features.
# The app works perfectly fine without Supabase!
#
# To enable cloud sync:
# 1. Create a free Supabase project at https://supabase.com
# 2. Copy your project URL and anon key
# 3. Create a .env file (copy this .env.example)
# 4. Fill in your values below

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Database Schema (run this SQL in Supabase SQL Editor):
#
# -- Enable Row Level Security
# CREATE TABLE sessions (
#   id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
#   user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE,
#   energy_level TEXT NOT NULL CHECK (energy_level IN ('low', 'medium', 'high')),
#   activity TEXT NOT NULL CHECK (activity IN ('study', 'rest', 'reflect')),
#   duration_minutes INTEGER,
#   feedback TEXT,
#   created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
# );
#
# -- Enable RLS
# ALTER TABLE sessions ENABLE ROW LEVEL SECURITY;
#
# -- Users can only see their own sessions
# CREATE POLICY "Users can view their own sessions"
#   ON sessions FOR SELECT
#   USING (auth.uid() = user_id);
#
# -- Users can insert their own sessions
# CREATE POLICY "Users can insert their own sessions"
#   ON sessions FOR INSERT
#   WITH CHECK (auth.uid() = user_id);
